name: rw

on:
  workflow_call:
    inputs:
      param1:
        description: "param1"
        required: true
        type: string

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Download artifact
        run: exit 1
